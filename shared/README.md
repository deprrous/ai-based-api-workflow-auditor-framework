# Shared

This directory holds assets used across backend and frontend boundaries.

## Planned internal areas

- `contracts/` - shared schemas, DTOs, and cross-stack contracts.
- `fixtures/` - example payloads, sample artifacts, and future test fixtures.

Shared artifacts reduce drift between the API server, AI orchestration outputs, and the frontend client.
